int majorityElement(vector<int>& nums) {
        int count=0;
        int ans;
        for(int i:nums)
        {   
           if(count==0)
               ans=i;
           if(i==ans)
               count++;
            else 
                count--;
        }
        return ans;
        
        
        
    }
